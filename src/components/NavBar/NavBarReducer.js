// import * as types from "../../constants/actionTypes";

const NavBarReducer = (
  state = {
    isAdmin: false
  },
  action
) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default NavBarReducer;